<h1>Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <h2>
    <%= link_to restaurant.name.capitalize, restaurant_path(restaurant) %>
  </h2>

<% end %>

<i class="fa fa-plus" aria-hidden="true"><%= link_to " Add a new restaurant", new_restaurant_path %></i>
